---
title: "Netcode Recipes"
description: "Task-oriented Colyseus netcode patterns: predicted projectiles with predict.spawns, optimistic hit FX and pickups, dead-reckoned AI, cooldown clocks, teleports, and physics-engine prediction with predict.sim."
---
import { Callout, Tabs } from 'nextra/components'
import { SDKTabs } from "../../components/SDKTabs"

# Netcode Recipes

Task-oriented patterns that build on the [Client Prediction](/netcode/client-prediction), [Server Input](/netcode/server-input), and [Lag Compensation](/netcode/lag-compensation) APIs. Each recipe stands alone — jump to the one that matches what you're building.

<Callout type="info">
    **Available in Colyseus 0.18+.**
</Callout>

## Choosing a remote smoothing mode

`Predict.attachAll` smooths entities you don't control. Pick the mode by what the entity *is*:

- **`lerp`** — remote **players**. Faithful (renders exactly where the server put them, a little in the past). The default for anything you also want lag-compensated, because rewind can match the interpolated instant.
- **`reckon`** — **AI / NPCs** you can forward-simulate from a known step function. Renders at ≈ `serverNow`, hiding latency; attach the server group with `mode: "reckon"` so hits line up.
- **`damped`** — **cosmetic** entities where "never jittery" beats "exact" (floating pickups, ambient critters). No fixed delay, so it can't be exactly lag-compensated.

Full behavior reference: [the modes table](/netcode/client-prediction#remote-entities).

## Taps between steps: buffer, then consume

Input is sampled once per **fixed step**, not per render frame — and frames and steps don't line up. A 120 Hz display on a 30 Hz tick runs a step every ~4th frame, and a hitchy frame can run several steps at once. Two traps follow: a tap on a **0-step frame** must not be lost (it belongs to the next step), and a tap spanning a **multi-step frame** must not fire twice. "Is the key down right now?" sampling gets both wrong.

**Buffer the press when it happens; consume it inside the step loop on exactly one step:**

<SDKTabs>

    <Tabs.Tab>
    ```ts
    let jumpBuffered = false;
    onKeyDown("Space", () => jumpBuffered = true);  // buffer: the tap happened

    function frame(now) {
      const n = predict.tick(now);
      for (let i = 0; i < n; i++) {
        input.data.moveX = readAxis();    // held state: sample live, no buffer
        input.data.jump = jumpBuffered;
        jumpBuffered = false;             // consume: fires on exactly one step
        input.send();
      }
    }
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```cs
    bool jumpBuffered = false;
    void OnJumpPressed() => jumpBuffered = true;    // buffer: the tap happened

    void Update() {
        int steps = predict.Tick();
        for (int i = 0; i < steps; i++) {
            cmd.moveX = ReadAxis();       // held state: sample live, no buffer
            cmd.jump = jumpBuffered;
            jumpBuffered = false;         // consume: fires on exactly one step
            input.Send();
        }
    }
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```lua
    local jump_buffered = false

    function on_input(self, action_id, action)
      if action_id == hash("jump") and action.pressed then
        jump_buffered = true              -- buffer: the tap happened
      end
    end

    function update(self, dt)
      local steps = predict:tick()
      for _ = 1, steps do
        input.data.moveX = read_axis()    -- held state: sample live, no buffer
        input.data.jump = jump_buffered
        jump_buffered = false             -- consume: fires on exactly one step
        input:send()
      end
    end
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```haxe
    var jumpBuffered = false;
    onKeyDown(Space, () -> jumpBuffered = true);  // buffer: the tap happened

    function frame() {
        var steps = predict.tick();
        for (_ in 0...steps) {
            cmd.moveX = readAxis();       // held state: sample live, no buffer
            cmd.jump = jumpBuffered;
            jumpBuffered = false;         // consume: fires on exactly one step
            input.send();
        }
    }
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```gdscript
    var jump_buffered := false

    func _input(event):
        if event.is_action_pressed("jump"):
            jump_buffered = true            # buffer: the tap happened

    func _process(_delta):
        var steps: int = predict.tick()
        for i in steps:
            input.data.moveX = read_axis()  # held state: sample live, no buffer
            input.data.jump = jump_buffered
            jump_buffered = false           # consume: fires on exactly one step
            input.send()
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```dart
    var jumpBuffered = false;
    onKeyDown('Space', () => jumpBuffered = true);  // buffer: the tap happened

    void onFrame() {
      Colyseus.pump();
      final steps = predict.tick();
      for (var i = 0; i < steps; i++) {
        input.data['moveX'] = readAxis();   // held state: sample live, no buffer
        input.data['jump'] = jumpBuffered;
        jumpBuffered = false;               // consume: fires on exactly one step
        input.send();
      }
    }
    ```
    </Tabs.Tab>

</SDKTabs>

Variants of the same shape:

- **Held buttons** (movement, auto-fire) sample the live state directly — no buffer.
- **Buffer windows** (a jump buffer with a grace period): buffer a timestamp instead of a flag; the consuming step decides whether it's still fresh.
- **Analog deltas** (pointer aim, mouselook): accumulate into a pending total and let each step consume its budget (`pending -= taken`) — fast motion carries over to later steps instead of clipping.

Buffer outside the loop, consume inside it — draining an edge anywhere else either drops taps (0-step frames) or double-fires them (multi-step frames).

## Unreliable input over WebTransport

On a datagram-capable transport, input frames don't need reliable delivery — each packet carries the last few inputs as redundancy, so a dropped datagram costs nothing and nothing head-of-line blocks.

<Callout type="warning">
    **Experimental, and WebTransport only.** Unreliable input needs a datagram channel, which only [WebTransport](/server/transport/webtransport) provides today — itself experimental. Right now only the JavaScript/TypeScript SDK supports WebTransport, and with it unreliable input.

    Other SDKs **reject** `mode: "unreliable"` at construction for now — over their WebSocket-only transports the redundancy ring would cost bandwidth for no benefit. Use `mode: "reliable"` there.
</Callout>

<SDKTabs>

    <Tabs.Tab>
    ```ts
    const input = room.input({ type: MoveInput, mode: "unreliable", historySize: 4 });
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```cs
    // Not available yet — Room.Input() rejects mode "unreliable"
    // (WebSocket-only transport). Use "reliable".
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```lua
    -- Not available yet — room:input() rejects mode "unreliable"
    -- (WebSocket-only transport). Use "reliable".
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```haxe
    // Not available yet — room.input() rejects mode "unreliable"
    // (WebSocket-only transport). Use "reliable".
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```gdscript
    # Not available — the Godot SDK sends input over the reliable channel only.
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```dart
    // Not available yet — room.input() throws on InputMode.unreliable
    // (WebSocket-only transport). Use InputMode.reliable.
    ```
    </Tabs.Tab>

</SDKTabs>

That's the whole migration. The framework runs its own sequence numbers and acks by seq **value**, so rollback reconciliation works unchanged — no user-side bookkeeping, no seq field on your schema. `historySize` is the redundancy depth (how many recent inputs ride along in each datagram; default 3).

## Predicted projectiles: `predict.spawns`

A fired bullet needs zero-latency feedback, but the server assigns its identity ~RTT later. `predict.spawns` runs the whole lifecycle: render the predicted local instantly, correlate it to the authoritative entity when it arrives, and keep one **stable id** across the handoff so the sprite never flickers:

<SDKTabs>

    <Tabs.Tab>
    ```ts
    const rockets = predict.spawns("rockets", {
      owned:     r => r.owner === room.sessionId,      // which server entities are mine
      spawnTime: r => r.bornMs,                        // exact per-shot input lead
      step:      stepRocket,                           // shared client/server sim
      fields:    ["x", "z"],                           // reckon confirmed entities
    });

    // on the predicted fire (live input step):
    rockets.spawn({ x, z, heading });

    // render — one path, handoff-invisible, keyed on the stable entry id:
    for (const e of rockets.entries()) {
      draw(e.id, rockets.value(e, "x"), rockets.value(e, "z"));
    }
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```cs
    var rockets = predict.Spawns<Rocket, Shot>("rockets",
        new PredictedSpawnsOptions<Rocket, Shot> {
            Owned     = r => r.owner == room.SessionId,     // which server entities are mine
            SpawnTime = r => r.bornMs,                      // exact per-shot input lead
            Step      = (shot, dt) => StepRocket(shot, dt), // shared client/server sim
            Fields    = new[] { "x", "z" },                 // reckon confirmed entities
        });

    // on the predicted fire (live input step):
    rockets.Spawn(new Shot { x = x, z = z, heading = heading });

    // render — one path, handoff-invisible, keyed on the stable entry id:
    foreach (var e in rockets.Entries()) {
        Draw(e.Id, rockets.Value(e, "x"), rockets.Value(e, "z"));
    }
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```lua
    local rockets = predict:spawns("rockets", {
      owned      = function(r) return r.owner == room.session_id end,  -- mine?
      spawn_time = function(r) return r.bornMs end,   -- exact per-shot input lead
      step       = step_rocket,                       -- shared client/server sim
      fields     = { "x", "z" },                      -- reckon confirmed entities
    })

    -- on the predicted fire (live input step):
    rockets:spawn({ x = x, z = z, heading = heading })

    -- render — one path, handoff-invisible, keyed on the stable entry id:
    for _, e in ipairs(rockets:entries()) do
      draw(e.id, rockets:value(e, "x"), rockets:value(e, "z"))
    end
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```haxe
    var rockets = predict.spawns("rockets", {
        owned:     (r) -> r.owner == room.sessionId,   // which server entities are mine
        spawnTime: (r) -> r.bornMs,                    // exact per-shot input lead
        step:      stepRocket,                         // shared client/server sim
        fields:    ["x", "z"],                         // reckon confirmed entities
    });

    // on the predicted fire (live input step):
    rockets.spawn({ x: x, z: z, heading: heading });

    // render — one path, handoff-invisible, keyed on the stable entry id:
    for (e in rockets.entries()) {
        draw(e.id, rockets.value(e, "x"), rockets.value(e, "z"));
    }
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```gdscript
    var rockets = predict.spawns("rockets", {
        "owned":      func(r): return r.owner == room.get_session_id(),
        "spawn_time": func(r): return r.bornMs,     # exact per-shot input lead
        "step":       _step_rocket,                 # shared client/server sim
        "fields":     ["x", "z"],                   # reckon confirmed entities
    })

    # on the predicted fire (live input step):
    rockets.spawn({ "x": x, "z": z, "heading": heading })

    # render — one path, handoff-invisible, keyed on the stable entry id:
    for e in rockets.entries():
        draw(e.id, rockets.value(e.id, "x"), rockets.value(e.id, "z"))
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```dart
    final rockets = predict.spawns('rockets', SpawnsOptions(
      owned:     (r) => r['owner'] == room.sessionId,       // which server entities are mine
      spawnTime: (r) => (r['bornMs'] as num).toDouble(),    // exact per-shot input lead
      step:      (local, dt) => stepRocket(local, dt),      // shared client/server sim
      fields:    const ['x', 'z'],                          // reckon confirmed entities
    ));

    // on the predicted fire (live input step):
    rockets.spawn(Shot(x, z, heading));

    // render — one path, handoff-invisible, keyed on the stable entry id:
    for (final e in rockets.entries) {
      draw(e.id, rockets.value(e, 'x'), rockets.value(e, 'z'));
    }
    ```
    </Tabs.Tab>

</SDKTabs>

With `spawnTime`, an **owned** projectile keeps flying the *shooter's* timeline through the handoff — no snap-back, and the rendered trajectory is the one a favor-the-shooter rewind actually judges. Foreign projectiles (other players') render at server-present, aged forward automatically. An unmatched prediction expires after a TTL (`max(2×rtt, 600ms)` by default) and `onReject` fires — the ghost reverts.

For fast projectiles, hit-test the swept **segment** between last and current position, not a point, so a frame-step doesn't tunnel through a thin target.

## Optimistic hit FX: `defineEvent`

Impact feedback (hit marker, sound, decal) shouldn't wait for the server, but a mispredicted hit must clean up after itself. Key the channel by projectile so multiple in-flight shots settle independently:

<SDKTabs>

    <Tabs.Tab>
    ```ts
    const impacts = predict.defineEvent<{ projectileId: string; x: number; y: number }>({
      uniqueBy:  (h) => h.projectileId,          // payload carries FX data; identity is the projectile
      onPredict: (h) => spawnImpactFx(h.x, h.y),
      onReject:  (h) => removeImpactFx(h.projectileId),
    });

    // inside the step, on a predicted impact:
    if (hit) ctx.predict(impacts, { projectileId: id, x: hit.x, y: hit.y });

    // settlement:
    room.onMessage("impact", (m) => impacts.confirm(m.projectileId));
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```cs
    var impacts = predict.DefineEvent(new PredictedEventChannelOptions<Hit> {
        UniqueBy  = h => h.projectileId,          // payload carries FX data; identity is the projectile
        OnPredict = h => SpawnImpactFx(h.x, h.y),
        OnReject  = h => RemoveImpactFx(h.projectileId),
    });

    // inside the step, on a predicted impact:
    if (hit) ctx.Predict(impacts, new Hit { projectileId = id, x = hit.x, y = hit.y });

    // settlement:
    room.OnMessage<Dictionary<string, object>>("impact",
        m => impacts.Confirm((string)m["projectileId"]));
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```lua
    local impacts = predict:define_event({
      unique_by  = function(h) return h.projectileId end,  -- identity is the projectile
      on_predict = function(h) spawn_impact_fx(h.x, h.y) end,
      on_reject  = function(h) remove_impact_fx(h.projectileId) end,
    })

    -- inside the step, on a predicted impact:
    if hit then ctx:predict(impacts, { projectileId = id, x = hit.x, y = hit.y }) end

    -- settlement:
    room:on_message("impact", function(m) impacts:confirm(m.projectileId) end)
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```haxe
    var impacts = predict.defineEvent({
        uniqueBy:  (h) -> h.projectileId,          // payload carries FX data; identity is the projectile
        onPredict: (h) -> spawnImpactFx(h.x, h.y),
        onReject:  (h) -> removeImpactFx(h.projectileId),
    });

    // inside the step, on a predicted impact:
    if (hit) ctx.predict(impacts, { projectileId: id, x: hit.x, y: hit.y });

    // settlement:
    room.onMessage("impact", (m) -> impacts.confirm(m.projectileId));
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```gdscript
    # string-keyed: the projectile id IS the entry identity (no uniqueBy, no payloads);
    # FX coordinates come from your own app state.
    var impacts = predict.define_event({
        "on_predict": func(id): spawn_impact_fx_for(id),
        "on_reject":  func(id): remove_impact_fx(id),
    })

    # inside the step, on a predicted impact:
    if hit: ctx.predict(impacts, id)

    # settlement:
    room.message_received.connect(func(type, m):
        if type == "impact": impacts.confirm(m.projectileId))
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```dart
    // string-keyed: the projectile id IS the entry identity (no uniqueBy, no payloads);
    // FX coordinates come from your own app state.
    final impacts = predict.defineEvent(
      onPredict: (id) => spawnImpactFxFor(id),
      onReject:  (id) => removeImpactFx(id),
    );

    // inside the step, on a predicted impact:
    if (hit) ctx.predict(impacts, id);

    // settlement:
    room.onMessage('impact').listen((m) => impacts.confirm(m['projectileId']));
    ```
    </Tabs.Tab>

</SDKTabs>

A sim-born prediction auto-rejects once the server processes past its birth step without confirming — see [Optimistic events](/netcode/client-prediction#optimistic-events-defineevent).

## Optimistic pickups & breakables: `confirmOn`

Kills, pickups, and breakables share one shape: predict a flag keyed by entity id, derive the visual from `has(id)` OR'd with authoritative state, and confirm when the entity's field flips. You declare the settlement; the channel does the wiring:

```ts
const pickups = predict.defineEvent<string>({
  confirmOn: { collection: "items", field: "alive", equals: false },
  // a remote player's pickup (nothing pending) — play the FX we skipped
  onUnpredicted: (id) => {
    const item = state.items.get(String(id));
    if (item) burstAt(item.x, item.y);
  },
});

// inside the step, on predicted contact:
if (touching) ctx.predict(pickups, itemId);

// render — hidden the instant WE predict it, or when the server says so:
const hidden = !item.alive || pickups.has(id);
```

A mispredict needs no cleanup code: the entry auto-rejects once the server processes past it silently, `has(id)` flips back, the derived view recovers. See [`confirmOn`](/netcode/client-prediction#confirming-from-state-confirmon) for the `add`/`remove` membership variants (predicted drops and consumables).

<Callout type="info">
    `confirmOn` is JavaScript-only. Ports get the same behavior with two lines: `ctx.predict(pickups, itemId)` in the step, and `confirm(itemId)` from a schema callback watching `alive`. The `has(id)` derive works everywhere except the GDScript binding.
</Callout>

## Dead-reckoned AI (server-driven)

For entities nobody controls directly but that move predictably (patrolling AI, ballistics), pair the client's `reckon` display with a `reckon` rewind timeline — both sides describe the same decision:

<SDKTabs>

    <Tabs.Tab>
    ```ts
    predict.attachAll("enemies", { mode: "reckon", fields: ["x", "y"], step: stepEnemy });
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```cs
    predict.AttachAll("enemies", new ReckonOptions<Enemy> {
        Fields = new[] { "x", "y" },
        Step = (e, dt, elapsedMs) => StepEnemy(e, dt),
    });
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```lua
    predict:attach_all("enemies", { mode = "reckon", fields = { "x", "y" }, step = step_enemy })
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```haxe
    predict.attachAll("enemies", { mode: "reckon", fields: ["x", "y"], step: stepEnemy });
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```gdscript
    predict.attach_all_reckon("enemies", ["x", "y"], _step_enemy)
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```dart
    predict.attachAllReckon('enemies', const ['x', 'y'], stepEnemy);
    ```
    </Tabs.Tab>

</SDKTabs>

The server rewinds on the matching timeline (no double compensation):

```ts
this.rewind.attachAll(this.state.enemies, { fields: ["x", "y"], mode: "reckon" });
```

For discrete movers (teleporters), keep snaps sharp under rewind with `interpolate`:

```ts
this.rewind.attachAll(this.state.enemies, {
  fields: ["x", "y"],
  interpolate: (e) => (e.kind === "teleporter" ? "step" : "linear"),
});
```

## Teleports & respawns: `snap`

Smoothing turns a respawn into a glide across the map. The `snap` option (on any attach or reconciler config) declares a value-space threshold past which a jump is a **teleport** — smoothing resets and the entity pops to the new value:

<SDKTabs>

    <Tabs.Tab>
    ```ts
    // Remotes: a passive attach pops past the threshold.
    predict.attachAll("players", { mode: "lerp", fields: ["x", "y"], snap: 300 });

    // Your own entity: a reconcile correction past it pops instead of gliding
    // (and doesn't count toward divergence telemetry).
    const me = predict.reconciler(self, { input, step, snap: 300 });
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```cs
    // Remotes: a passive attach pops past the threshold.
    predict.AttachAll("players", new AttachConfig {
        ["x"] = new PredictFieldOptions { Mode = PredictMode.Lerp, Snap = 300 },
        ["y"] = new PredictFieldOptions { Mode = PredictMode.Lerp, Snap = 300 },
    });

    // Your own entity: a reconcile correction past it pops instead of gliding.
    var me = predict.Reconciler(self, new ReconcilerOptions<Player, MoveInput> {
        Input = input,
        Step = (ctx, s, cmd) => ApplyInput(s, cmd, ctx.Dt),
        Snap = 300,
    });
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```lua
    -- Remotes: a passive attach pops past the threshold.
    predict:attach_all("players", {
      x = { mode = "lerp", snap = 300 },
      y = { mode = "lerp", snap = 300 },
    })

    -- Your own entity: a reconcile correction past it pops instead of gliding.
    local me = predict:reconciler(self_player, { input = input, step = step, snap = 300 })
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```haxe
    // Remotes: a passive attach pops past the threshold.
    predict.attachAll("players", {
        x: { mode: "lerp", snap: 300 },
        y: { mode: "lerp", snap: 300 },
    });

    // Your own entity: a reconcile correction past it pops instead of gliding.
    var me = predict.reconciler(self, { input: input, step: step, snap: 300 });
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```gdscript
    # Remotes: a passive attach pops past the threshold.
    predict.attach_all("players", {
        "x": { "mode": Colyseus.Predict.LERP, "snap": 300 },
        "y": { "mode": Colyseus.Predict.LERP, "snap": 300 },
    }, room.get_session_id())

    # Your own entity: a reconcile correction past it pops instead of gliding.
    var me = predict.reconciler(self_player, {
        "input": input, "step": _step, "snap": 300,
    })
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```dart
    // Remotes: a passive attach pops past the threshold.
    predict.attachAll('players', config: {
      'x': FieldOptions(mode: PredictMode.lerp, snap: 300),
      'y': FieldOptions(mode: PredictMode.lerp, snap: 300),
    }, exceptKey: room.sessionId);

    // Your own entity: a reconcile correction past it pops instead of gliding.
    final me = predict.reconciler(self, input: input, step: step, snap: 300);
    ```
    </Tabs.Tab>

</SDKTabs>

Size it well above per-patch motion (`maxSpeed × patchInterval`) and below your smallest legitimate teleport. Respawns that land on the same position induce zero correction and never trip it.

## Cooldowns & timers: pick the right clock

Three kinds of "wait", three homes:

- **Self-imposed rate limits** (fire rate, ability spam guard the client enforces on itself) — `room.clock.now()`, local monotonic: `now() - lastFired >= COOLDOWN_MS`. No clock sync involved, so no jitter.
- **Server-stamped windows** (invulnerable-until, respawn-at, round-end) — compare against `room.clock.serverNow()`; the server wrote the absolute deadline.
- **Cooldowns that gate the simulation itself** (a dash the step must refuse during cooldown) — make the cooldown a **reconciled field ticked inside `step`** (`dashCooldown--` per tick, in `fields`). It predicts, replays, and self-corrects like any other simulated state; a wall-clock gate inside `step` would break replay.

## Physics-engine prediction (`predict.sim` + Rapier)

When the local simulation is an opaque engine, bridge it into `predict.sim`'s `step` — the engine handle is your `world`. On the server, consume one input per tick with `next()` and step the world once:

```ts
// server
step(ctx) {
  for (const [sid, body] of this.bodies) {
    const cmd = this.inputs.get(sid).next();
    if (cmd) applyInput(body, cmd);
  }
  this.world.step(ctx.dt);
}
```

```ts
// client — same applyInput, same dt
const me = predict.sim({
  input,
  world: { world, body },
  step:  (ctx, w, cmd) => { applyInput(w.body, cmd); w.world.step(ctx.dt); },
  adopt: (w) => { w.body.setTranslation({ x: self.x, y: self.y }, true);
                  w.body.setLinvel({ x: self.vx, y: self.vy }, true); },
  pose:  (w) => { const t = w.body.translation(); return { x: t.x, y: t.y }; },
});
```

<Callout type="info">
    The opaque-engine face (`adopt` + `pose`) is JavaScript-only — see [the sim note](/netcode/client-prediction#composite--engine-state-predictsim). On the other SDKs, keep engine state in bound schema mirrors — see [Composite scalars](#composite-scalars-predictsim-no-engine) below.
</Callout>

<Callout type="warning">
    Reseed **every** scalar the sim depends on in `adopt` (position **and** velocity). Engine-internal non-scalar state isn't rolled back — see [the warning on `predict.sim`](/netcode/client-prediction#composite--engine-state-predictsim).
</Callout>

## Composite scalars (`predict.sim`, no engine)

When an input pushes more than your own entity — a paddle that strikes a puck — put the decoded schema instances in the `world` and they **auto-bind**: no `adopt` or `pose` callbacks, and the bound entities render through the ordinary `predict.value(instance, field)` read. See [Composite & engine state](/netcode/client-prediction#composite--engine-state-predictsim).

## Hand-rolled simulation (no physics engine)

You don't need an engine. The simplest way to satisfy [the determinism contract](/netcode/determinism) is plain math: `applyInput` integrates velocity and gravity by hand, shared verbatim between client and server — no engine build to keep in sync.

## Next

→ [Determinism & The Contract](/netcode/determinism) — the rules these patterns rely on, and the troubleshooting table.<br/>
→ [Client Prediction](/netcode/client-prediction) — the full API reference behind every recipe here.
