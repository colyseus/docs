---
title: "Colyseus - Multiplayer Game Framework for Node.js"
description: "Colyseus is an open-source multiplayer framework for Node.js. Build authoritative game servers with real-time state synchronization, matchmaking, built-in client prediction, and SDKs for every major game engine."
# toc: false
---

import Image from 'next/image'
import { Callout, Steps, Tabs, Cards } from 'nextra/components'
import { CodeIcon, PeopleIcon, SyncIcon, ServerIcon, CloudIcon, DownloadIcon, RepoIcon, FileCodeIcon, ZapIcon, DevicesIcon, PasskeyFillIcon } from '@primer/octicons-react'
import { ScenarioGrid, ScenarioCard } from '../components/scenario-card'

{/* <Image src={require('../images/hero-christmas.webp')} width={0} height={0} style={{ width: '100%', height: 'auto' }} alt="Colyseus Hero Image" /> */}
<Image src={require('../images/hero.png')} width={0} height={0} style={{ width: '100%', height: 'auto' }} alt="Colyseus Hero Image" />

# Colyseus Multiplayer Framework

Colyseus is an open-source Node.js framework for building authoritative game servers — with real-time state synchronization, matchmaking, and effortless integration into any game engine or frontend.

## Key Features

<ScenarioGrid>
    <ScenarioCard icon={<PeopleIcon size={18}/>} title="Rooms & Matchmaking" href="/room">
        From a single Room definition, clients are matched into multiple room instances — with visibility flags, lobbies, and ranked queues.
    </ScenarioCard>
    <ScenarioCard icon={<SyncIcon size={18}/>} title="State Synchronization" href="/state">
        Define your state on the server; clients receive binary delta patches automatically.
    </ScenarioCard>
    <ScenarioCard icon={<ZapIcon size={18}/>} title="Prediction-Ready Netcode" href="/netcode">
        Built-in client prediction, rollback, interpolation, and lag compensation — new in 0.18.
    </ScenarioCard>
    <ScenarioCard icon={<CodeIcon size={18}/>} title="Full-Stack Type Safety" href="/sdk">
        Share your server's types with the SDK: typed messages, typed join options, typed state.
    </ScenarioCard>
    <ScenarioCard icon={<PasskeyFillIcon size={18}/>} title="Batteries Included" href="/auth">
        Authentication (JWT + OAuth), database services, room plugins, admin panel, and dev tools out of the box.
    </ScenarioCard>
    <ScenarioCard icon={<DevicesIcon size={18}/>} title="Any Engine" href="/getting-started">
        Official SDKs for the Web, Unity, Godot, Defold, GameMaker, Construct, Haxe, and more.
    </ScenarioCard>
    <ScenarioCard icon={<ServerIcon size={18}/>} title="Scale & Deploy Anywhere" href="/scalability">
        Horizontal scaling across processes. Self-host for free, or use Colyseus Cloud for managed hosting.
    </ScenarioCard>
</ScenarioGrid>

## What does it look like?

<Steps>

### Create a server

Create and start a new Colyseus server using the following commands:

<Tabs items={['Node.js', 'Bun']}>

  <Tabs.Tab>
```sh filename="Terminal: Node.js"
# Create a new Colyseus project
npm create colyseus-app@latest ./my-server

# Enter the project directory
cd my-server

# Run the server
npm start
```
  </Tabs.Tab>

  <Tabs.Tab>
```sh filename="Terminal: Bun"
# Create a new Colyseus project
bun create colyseus-app@latest ./my-server

# Enter the project directory
cd my-server

# Install Bun transport & Run the server
bun add @colyseus/bun-websockets
bun run src/index.ts
```
  </Tabs.Tab>
</Tabs>

### Define your Room State

The Room state is the data structure that will be synchronized between the server and the connected clients:

```ts filename="src/rooms/MyState.ts"
import { Schema, MapSchema, type } from "@colyseus/schema";

export class Player extends Schema {
    @type("number") x: number = 0;
    @type("number") y: number = 0;
}

export class MyState extends Schema {
    @type({ map: Player }) players = new MapSchema<Player>();
}
```

### Create your Room code

Define the game logic and client interactions with the game state within your Room code.

```ts filename="src/rooms/MyRoom.ts"
import { Room, Client } from "colyseus";
import { MyState, Player } from "./MyState";

export class MyRoom extends Room {
    maxClients = 4;
    state = new MyState();

    messages = {
        doSomething: (client, payload) => {
            // handle client's "doSomething" message
        }
    }

    // Called when the room is created
    onCreate(options) { }

    // Called when a client joins the room
    onJoin(client: Client, options: any) {
        this.state.players.set(client.sessionId, new Player());
    }

    // Called when a client leaves the room
    onLeave(client: Client, code: number) {
        this.state.players.delete(client.sessionId);
    }

    // Called when the room is disposed
    onDispose() { }
}
```

### Expose the Room identifier

Exposing the room type allows clients to connect to the server and dynamically create instances of it.

```ts {6} filename="src/app.config.ts"
import { defineServer, defineRoom } from "colyseus";
import { MyRoom } from "./MyRoom";

const server = defineServer({
    rooms: {
        my_room: defineRoom(MyRoom),
    },
});
```

### Join the Room from the client SDK

Communication between the client and server occurs through room connections. Below are examples for each SDK showing how to join a room and listen for state changes:

<Tabs items={["TypeScript", "Unity (C#)", "Defold (Lua)", "Haxe"]} storageKey='sdk-tab-language'>

    <Tabs.Tab>
    ```ts filename="client.ts"
    import { Client, Callbacks } from "@colyseus/sdk";

    async function connect() {
        const client = new Client('http://localhost:2567');
        const room = await client.joinOrCreate('my_room', {
            /* custom join options */
        });
        const callbacks = Callbacks.get(room);

        // Listen to 'player' instance additions
        callbacks.onAdd("players", (player, sessionId) => {
            console.log('Player joined:', player);
        });

        // Listen to 'player' instance removals
        callbacks.onRemove("players", (player, sessionId) => {
            console.log('Player left:', player);
        });

        // Send messages to the server
        room.send("doSomething", {/* ... */});

        return room;
    }

    connect();
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```cs filename="Client.cs"
    using Colyseus;

    async void Connect() {
        var client = new Client("http://localhost:2567");

        try {
            var room = await client.JoinOrCreate<MyRoomState>("my_room");
            var callbacks = Colyseus.Schema.Callbacks.Get(room);

            // Listen to 'player' instance additions
            callbacks.OnAdd(state => state.players, (sessionId, player) => {
                Debug.Log($"Player joined: {sessionId}");
            });

            // Listen to 'player' instance removals
            callbacks.OnRemove(state => state.players, (sessionId, player) => {
                Debug.Log($"Player left: {sessionId}");
            });

            // Send messages to the server
            room.Send("doSomething", new { /* ... */ });

        } catch (Exception ex) {
            Debug.Log("join error");
            Debug.Log(ex.Message);
        }
    }
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```lua filename="client.lua"
    local ColyseusSDK = require("colyseus.sdk")

    local client = ColyseusSDK.Client("http://localhost:2567")

    client:join_or_create("my_room", {--[[custom join options]]}, function(err, room)
        if (err ~= nil) then
            print("join error: " .. err)
            return
        end

        local callbacks = ColyseusSDK.callbacks(room)

        -- Listen to 'player' instance additions
        callbacks:on_add("players", function(player, sessionId)
            print("Player joined:", sessionId)
        end)

        -- Listen to 'player' instance removals
        callbacks:on_remove("players", function(player, sessionId)
            print("Player left:", sessionId)
        end)

        -- Send messages to the server
        room:send("doSomething", {--[[ ... ]]})
    end)
    ```
    </Tabs.Tab>

    <Tabs.Tab>
    ```haxe filename="Client.hx"
    import io.colyseus.Client;
    import io.colyseus.serializer.schema.Callbacks;

    class Main {
        static function main() {
            var client = new Client("http://localhost:2567");

            client.joinOrCreate("my_room", [], MyRoomState, function(err, room) {
                if (err != null) {
                    trace("join error: " + err);
                    return;
                }

                var callbacks = Callbacks.get(room);

                // Listen to 'player' instance additions
                callbacks.onAdd("players", function(player, sessionId) {
                    trace("Player joined: " + sessionId);
                });

                // Listen to 'player' instance removals
                callbacks.onRemove("players", function(player, sessionId) {
                    trace("Player left: " + sessionId);
                });

                // Send messages to the server
                room.send("doSomething", [/* ... */]);
            });
        }
    }
    ```
    </Tabs.Tab>

</Tabs>

### Explore more

Explore more about Colyseus by following the tutorials and example projects:

<Cards>
    <Cards.Card icon={<RepoIcon/>} title="Example Projects" href="/learn/examples" arrow />
    <Cards.Card icon={<FileCodeIcon/>} title="Tutorials" href="/learn/tutorial" arrow />
</Cards>

</Steps>
