.next
out
node_modules
.cache
.DS_Store
GH_TOKEN
*.psd

# working notes and drafts, deliberately kept out of git
TODO/

# generated by `npm run generate:ai-nav` (runs as part of `build`).
# `lastmod` comes from git history, so a committed copy is always one commit
# behind — see GENERATE_ONLY in scripts/generate-ai-nav.js.
public/sitemap.xml

# incremental tsc cache
tsconfig.tsbuildinfo
